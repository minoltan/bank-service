package com.example.bankservice;

/**
 * @author Minoltan Issack on 7/23/2022
 */
public class TransactionTest {

    // Register my savings account - userInfo, accountInfo (uuid, bal = 0) - USD User

    // Withdraw case 1 - no balance
    // Withdraw case 2 - wrong user
    // Withdraw case 3 - happy path after deposit (reflect in transaction, accountInfo table)


    // Deposit case 1 - wrong user
    // Deposit case 2 - correct user - if deposit 2 Euro - convert to USD
    // no currency table error

    // Register my current account - userInfo, accountInfo (uuid, bal = 0) - USD User
    // Register my savings account - userInfo, accountInfo (uuid, bal = 0) - EURO User
    // Register my savings account - userInfo, accountInfo (uuid, bal = 0) - LKR User

}
